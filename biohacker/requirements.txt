aws-opentelemetry-distro>=0.10.0
boto3
litellm
mcp[cli]
nova-act
opensearch-py
pandas
retrying
strands
strands-agents
strands-agents-tools
tqdm
uv

# note: nova-act and some strands tools may be private; ensure network/credentials
# are available during docker build if required.

# Don't forget to create and activate your environment

# python -m venv .venv

## macOS / Linux: source .venv/bin/activate
## Windows (CMD): .venv\Scripts\activate.bat
## Windows (PowerShell): .venv\Scripts\Activate.ps1
# pip install -r requirements.txt

# Use AWS CLI, a bash script, or just manually set your environment variables
